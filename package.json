{
  "name": "season-8-modpack",
  "version": "1.0.0",
  "description": "If you make changes to the server mods or the client modpack, make sure to check and modify both if neccesary.",
  "scripts": {
    "build": "packwiz curseforge export",
    "format": "prettier --write . --ignore-unknown",
    "prepare": "husky install"
  },
  "author": "Farwater mod team",
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "2.8.7"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
